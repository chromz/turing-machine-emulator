{
  "main": "main.js",
  "scripts": {
    "build": "gulp",
    "start": "yarn build && ./node_modules/.bin/electron ."
  },
  "dependencies": {
    "ace-builds": "^1.3.3",
    "angular-animate": "^1.7.0",
    "electron": "^2.0.2",
    "gulp": "^3.9.1",
    "gulp-sass": "^4.0.1"
  }
}
